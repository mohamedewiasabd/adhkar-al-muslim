import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.muslim.adhkar.wird',
  appName: 'أذكار المسلم - الورد اليومي',
  webDir: 'dist'
};

export default config;
