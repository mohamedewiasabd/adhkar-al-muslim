# دليل الرفع على Google Play — أذكار المسلم (الورد اليومي)

كل ما تحتاجه للرفع جاهز ومولّد. اقرأ هذا الدليل كاملاً قبل البدء.

---

## 1) الملفات الجاهزة

| الملف | الاستخدام |
|---|---|
| `release/أذكار-المسلم-v1.0.0-release.aab` | **الرفع إلى Google Play** (ملف Android App Bundle الرسمي) |
| `release/أذكار-المسلم-v1.0.0-release.apk` | تثبيت مباشر/توزيع خارج المتجر (موقّع بنفس المفتاح) |
| `release/أذكار-المسلم-v1.0.0-debug.apk` | اختبار فقط — لا ترفعه للمتجر |
| `release/play-assets/icon-512x512.png` | أيقونة المتجر (يُفضّل رفع 512×512 بتنسيق PNG) |
| `release/play-assets/feature-graphic-1024x500.png` | غلاف المتجر (مطلوب) |
| `release/play-assets/screenshots/01..07.png` | صور شاشات الهاتف (7 لقطات 1080×1920 حقيقية من التطبيق) |
| `android/key.properties` + `android/keystore/adhkar-release.keystore` | **مفتاح التوقيع — سرّي للغاية** |

> ستظل الملفات العربية (باسم «أذكار-المسلم») تعمل بشكل طبيعي في المتجر؛ يمكنك إعادة التسمية بالأحرف اللاتينية `adhkar-muslim-v1.0.0-release.aab` إن أردت ثم رفعها.

---

## 2) المتطلبات قبل الرفع

1. **حساب مطوّر على Google Play** — رسوم تسجيل واحدة **25$**. على المنصة: play.google.com/console
2. **سياسة الخصوصية** — مطلوبة إلزامياً. انسخ الأنموذج في القسم (6) وارفعه على أي استضافة مجانية (GitHub Pages، Netlify، Notion...). ستحتاج **رابط URL** له.
3. **الإعلانات**: التطبيق **مجاني مع إعلانات AdMob** عبر 3 وحدات (إعلان عند فتح التطبيق، إعلان مكافأة، وحدات Native مدمجة بالمحتوى). ستحتاج محتوى سياسة خصوصية يذكرها ونموذج Data safety يعلن جمع البيانات الإعلانية (انظر أدناه).

---

## 3) خطوات Play Console (مرة واحدة لكل إصدار)

1. سجّل دخولك إلى [Google Play Console](https://play.google.com/console) → أنشئ تطبيق **Create app**:
   - الاسم (اسم المتجر): **أذكار المسلم — الورد اليومي**
   - اللغة الافتراضية: العربية
   - التطبيق أو اللعبة: **تطبيق**
   - مجاني أو مدفوع: **مجاني**
2. من القائمة الجانبية **Grow → Store presence → Overview**: تعبئة البيانات.

### 4) بيانات المتجر (Store listing)

- **وصف قصير** (≤80 حرفاً):
  `أذكار المسلم: أذكار الصباح والمساء والنوم وبعد الصلاة + مسبحة وورد يومي.`

- **الوصف الكامل** (≤4000 حرف):
```
تطبيق «أذكار المسلم — الورد اليومي» يذكّرك بالله في كل وقت، بأذكار موثوقة مقسّمة على مدار اليوم:

• أذكار الصباح والمساء
• أذكار النوم والاستيقاظ
• أذكار بعد الصلاة وأذكار المسجد
• أذكار الطعام والشراب وأذكار الوضوء
• أذكار السفر والمطر والريح
• أذكار الهم والحزن والكرب
• مسبحة إلكترونية بعداد ذكي (+ مكافأة بإعلان قصير تمنح +100 تسبيحة)
• الورد اليومي لتتبع تقدمك (141 ذِكراً يومياً + كل يوم أحد يعيد الورد)
• مصحف مبسط مع الفهرس
• أدعية متنوعة
• إحصائيات إنجازك وخططك الأسبوعية
• واجهة عربية كاملة مع وضع ليلي داكن مريح للعين
• إشعارات أذكار الصباح والمساء تعمل حتى بعد إغلاق التطبيق

التطبيق مجاني ومدعوم بإعلانات AdMob؛ تُعرض الإعلانات بموافقة المستخدم حسب سياسات محتوى ملائمة، ويمكن إعادة ضبط خيارات الخصوصية من داخل إعدادات النظام.

جميع بياناتك محفوظة على جهازك فقط — لا نطلب حساباً للاستخدام.

نسأل الله أن ينفعنا وإياكم، والتزموا بالأذكار الصحيحة وما صح عن النبي ﷺ.
```

- **الفئة**: Faith / الترفيه الديني وأسلوب الحياة
- **العلامات (Tags)**: إسلام، أذكار، صباح، مساء، سبحة، دين

### 5) نموذج الأمان والبيانات (Data safety) + تقييم المحتوى

- **Data safety**: أجِب بوجود إعلانات (AdMob). صرّح بما يلي في النموذج:
  - **يتم جمع البيانات**: معرّفات الأجهزة (cookies أو ما يعادلها، المعرّف الإعلاني) — تتم المشاركة مع Google لعرض الإعلانات وقياس أدائها.
  - **التطبيق و Google Mobile Ads SDK** يجمعان: معرّف الإعلان، نوع الجهاز، بيانات التفاعل مع الإعلانات (لأغراض الإعلانات).
  - اختياري: عدم جمع أي بيانات شخصية أخرى (بيانات التطبيق/الأذكار محلية بالكامل).
  - **سياسة الخصوصية** يجب أن تصرّح بوضوح باستخدام AdMob وروابط [سياسة خصوصية AdMob](https://support.google.com/admob/answer/6128543) و[سياسات Google الإعلانية](https://policies.google.com/privacy) واختيارات المستخدم (UMP).
- **Content rating**: خذ الاستبيان؛ النتيجة المتوقعة: **Everyone (الكل)** — محتوى ديني لجميع الأعمار.
- **Target audience**: الجمهور العام، مع إعلانات.
- **الموافقة على الإعلانات (UMP)**: التطبيق مدمج مع منصة Google للمستخدمين (UMP) — سيُطلب من المستخدمين في الاتحاد الأوروبي/المملكة المتحدة موافقة على الإعلانات المخصّصة تلقائياً عند أول تشغيل، ويمكنهم تعديل الخيارات لاحقاً من `إعدادات التنبيه والإعلانات` أو أيقونة خصوصية Google.
- **إذن الإشعارات (Notifications)**: التطبيق يستخدم إشعارات النظام المجدولة لإرسال أذكار الصباح والمساء بعد إغلاق التطبيق — يُطلب إذن `POST_NOTIFICATIONS` عند التفعيل فقط، وتُرفض الإشعارات إن لم يوافق المستخدم. هذا مندرج ضمن نموذج Data Safety دون ارتباط بالبيانات الإعلانية.

### 6) سياسة الخصوصية (نموذج جاهز — عرّب الرابط بعد الرفع)

```
سياسة الخصوصية لتطبيق «أذكار المسلم — الورد اليومي»

آخر تحديث: 15 سبتمبر 2026

خصوصيتكم مهمة لنا. يوضّح هذا المستند ما يجمعه التطبيق وكيف نستخدمه.

١) بيانات التطبيق الأساسية
لا يجمع التطبيق أي بيانات شخصية ضرورية للاستخدام: أذكارك، عدّاد
المسبحة، سجل الورد اليومي، والمفضلة تُحفظ محلياً على جهازك فقط ولا
تُنقل عبر الإنترنت.

٢) الإعلانات (AdMob)
التطبيق مجاني ومدعوم بإعلانات «Google AdMob». قد يجمع Google Mobile
Ads SDK معرّف الإعلان (Advertising ID)، نوع الجهاز، ونشاط التفاعل مع
الإعلانات لعرض إعلانات ملائمة وقياس الأداء ومنع الاحتيال. يتم ذلك وفق
سياسات Google للخصوصية: https://policies.google.com/privacy
وسياسة AdMob: https://support.google.com/admob/answer/6128543

٣) الموافقة على الإعلانات المخصّصة (UMP)
في الاتحاد الأوروبي والمملكة المتحدة، نعرض طلب موافقة من Google
(UMP) عند أول تشغيل؛ يمكنك رفض الإعلانات المخصّصة أو قبولها، وتغيير
خيارك لاحقاً من أيقونة خيارات الخصوصية داخل إعدادات التطبيق.

٤) الإشعارات
نستخدم إشعارات النظام المجدّدة لإرسال أذكار الصباح والمساء حتى بعد
إغلاق التطبيق، ولا تحوي بيانات خارجية.

٥) الخطوط
نستخدم خطوط Google Fonts عند اتصالك بالإنترنت لتحسين عرض النصوص.

٦) التواصل
لأي استفسار يرجى مراسلة: <ضع بريدك الإلكتروني>

— فريق التطبيق
```

### 7) رفع الملف للمتجر

1. من القائمة **Release → Production (أو Release → Testing → Closed testing)**
   > **نصيحة**: ابدأ بتجربة Closed testing مع نفسك ثم انقل للإنتاج لاحقاً.
2. اضغط **Upload** وارفع `release/أذكار-المسلم-v1.0.0-release.aab`
3. **Release notes**: اكتب بالعربية:
```
الإصدار الأول من التطبيق.

• أذكار الصباح والمساء والنوم والاستيقاظ وبعد الصلاة وما بعدها
• مسبحة إلكترونية وورد يومي ومصحف وأدعية
• واجهة عربية بوضع ليلي، وتوفير كامل لبطارية الجهاز
```
4. أرفق لقطات الشاشات والأيقونة والغلاف من القسم (4).
5. اضغط **Review release** ثم **Publish**.

---

## 8) الحفاظ على مفتاح التوقيع — تنبيه حرج

- مفتاحك: `android/keystore/adhkar-release.keystore` (الاسم المستعار `adhkar`).
- كلمة المرور محفوظة مؤقتاً في `/tmp/opencode/kspass.txt`.
- **انسخ الملف + كلمة المرور فوراً إلى مكانين آمنين** (محرك خارجي + سحابة مشفرة).
- فقدان المفتاح أو كلمة مروره = **استحالة تحديث تطبيقك** بعد النشر إلا بنقل للتطبيق.
- لا ترفع `key.properties` أو `keystore/` إلى أي مستودع عام (مضافة بالفعل إلى `.gitignore`).

### تفعيل Play App Signing (اختياري لكن موصى به)
في Console → Settings → App integrity، فعّل Play App Signing؛ فتُدار مفاتيحك من غوغل وتصبح مفتاح الرفع هو «Upload key».

---

## 9) بناء نسخ مستقبلية (النسخة 1.0.x ثم 1.1.0...)

بعد أي تعديل في كود الويب (مجلد `src/`):

```bash
npm run build          # بناء نسخة dist
npx cap sync android   # مزامنة الويب + أي إضافات Capacitor جديدة مع المشروع الأصلي
cd android
sed -i 's/versionName "1.0.0"/versionName "1.1.0"/' app/build.gradle
./gradlew assembleRelease bundleRelease
```

ثم ارفع ملف `app/build/outputs/bundle/release/app-release.aab` الجديد وزد **versionCode** (أيضاً في `build.gradle`) فوق القيمة السابقة في كل إصدار.

> ملاحظة: عند إضافة أي `@capacitor/*` جديد يجب `npx cap sync android` لدمج الأذونات/الاستقبالات تلقائياً (كما حدث مع إشعارات النظام).

---

## 10) الاختبار قبل النشر

بدون جهاز: لا مشكلة، المخرجات مجرّبة عامةً (البناء + التوقيع + فحص apksigner نجحوا).

مع جهاز Android (شغّل تصحيح USB ثم):

```bash
adb install -r release/أذكار-المسلم-v1.0.0-debug.apk   # نسخة اختبار
adb install -r release/أذكار-المسلم-v1.0.0-release.apk # النسخة النهائية
```

---

## 11) ملاحظات تقنية مهمة

- **التطبيق بلا إنترنت**: لا يتطلب إنترنت لعمل الأذكار. الاستثناء الأول: تحميل خطوط Google Fonts (Amiri/Cairo) عند أول تشغيل بنت متصلة لتحسين النصوص؛ وبدون الشبكة تعمل بخطوط النظام.
- **إشعارات ما بعد الإغلاق**: التطبيق يجدول إشعارات النظام (أذكار الصباح/المساء + ذكر دوري) تعمل حتى بعد إغلاق التطبيق وبعد إعادة تشغيل الهاتف (تستعيد برمجياً عند الـ Boot)، بشرط ألا يستخدم المستخدم خيار «Force stop» وألا يكتم تفعيل إشعارات النظام من الإعدادات.
- **الإعلانات (AdMob Next-Gen)**: ثلاث وحدات مُدمجة — إعلان عند عودة التطبيق للواجهة (App Open)، إعلان مكافأة يمنح +100 تسبيحة، وإعلان Native يظهر مدمجاً في قائمة الأذكار بعد الذكر الخامس. معرّف التطبيق مضبوط تلقائياً في Manifest. **للاختبار المحلي**: غيّر `TEST_ADS = true` في `src/utils/ads.ts` (يجعل جهازك جهازاً اختبارياً) ثم أعد البناء، وتأكد من إعادتها `false` قبل الرفع.
- **الحجم**: ~9MB الآن (بسبب تضمين Google Mobile Ads SDK)، بدل ~3.2MB سابقاً.
- **بيانات الورد اليومي** محفوظة محلياً (localStorage داخل WebView) — لا تختفي عند إغلاق التطبيق.
- **الحد الأدنى لنظام أندرويد**: 7.0 (API 24)؛ الهدف: أحدث (API 36).
- **الحجم**: ~3.2MB فقط — ميسّر للرفع.
- **التوثيق**: بصمة مفتاح التوقيع SHA-256:
  `81a08dcc951dd05ded8fceafd4424a6c5e6b8022ef01e48aab1853206ad6f179`
  (قد يُطلب SHA-1 أيضاً عند ملء نماذج إضافية: `fc646fc5802ad290e0f76dc300003c3ea2d2b246`).

---

## 12) قائمة فحص أخيرة قبل النشر

- [ ] الملف `*.aab` من قسم (7) مرتفع بنجاح ومراجعة المتجر لا تُظهر أخطاء
- [ ] سياسة الخصوصية معرّبة ومرفوعة وبالرابط مذكور في Console
- [ ] نموذج Data safety مكتمل (لا نجمع بيانات)
- [ ] استبيان Content rating مكتمل (الكل / Everyone)
- [ ] نسخة احتياطية من keystore + كلمة المرور في مكانين آمنين
- [ ] الأيقونة (512) والغلاف (1024×500) ولقطات الشاشات مرفوعة
- [ ] على جهاز حقيقي: فعّل إشعارات النظام من «إعدادات التنبيه» واختبر زر التجربة، ثم أغلق التطبيق وتأكد من وصول إشعار الصباح في وقته